# LEVH 2.26.x Comprehensive Hardening Loop

Status: active release-blocker remediation. New product features and the optional
LLM contradiction adapter remain blocked until every loop below closes green.

Each loop uses the same evidence cycle:

```text
reproduce → narrow fix → focused regression tests → full suite → package/build
→ fresh-extract smoke → update checkpoint
```

A loop does not advance on a failed invariant. Failures are reported with the
exact command and retained as the next loop's starting checkpoint.

## Loop 1 — 2.26.1 Config & Clean Release

- canonical runtime config resolver
- precedence: explicit > environment > local config > defaults
- custom DB path shared by CLI/API/MCP/doctor/onboarding
- React 19-compatible dependency graph
- clean `npm ci` contract shared by release, CI and Docker
- pinned Node/npm support range and static-export assertion

## Loop 2 — 2.26.2 Admission & Privacy Integrity

- admission-gated default write paths
- explicit audited raw/admin bypass only
- secret-safe audit previews
- localhost-safe Docker default and non-loopback token rule
- public privacy claims aligned with proven behavior

## Loop 3 — 2.26.3 Delete, Restore & Derived-State Integrity

- transactional two-phase restore
- complete derived-table deletion and audit
- entity/trust/conflict invalidation or deterministic reconciliation
- consolidation lineage/privacy propagation
- session count consistency

## Loop 4 — 2.26.4 Connector & Robustness

- chunk progress invariants and limits
- symlink/root boundary enforcement
- import rollback / no ghost cache entries
- UTC timestamp normalization
- embedding provenance and mixed-dimension diagnostics

## Loop 5 — 2.26.5 Black-box RC Validation

- installed-wheel CLI subprocess matrix
- MCP stdio/SSE protocol smoke
- clean frontend build from lockfile
- Docker build/runtime smoke
- deletion/privacy matrix
- fresh-machine quickstart and release-claim audit

Completion target: `2.27_PUBLIC_RELEASE_CANDIDATE_READY`.
