TASK-00A3 command chronology
worktree=<AUDIT_WORKTREE>

1. git rev-parse HEAD
   3a97ae7177c128e5484434d76828751330149fc3

2. git branch --show-current
   audit/groundtruth-v2

3. git status --short --untracked-files=all
   Only accepted TASK-00A1 and TASK-00A2 outputs were present before TASK-00A3.

4. Search for CODEX_TASK_00A3_UPDATE_ADMISSION_INVARIANT.md under Downloads.
   result=not found
   The user-provided mandatory matrix and surfaces were used.

5. Source survey
   rg/read for MemoryEngine.update_memory, admission/redaction, REST create/PUT,
   MCP update, CLI, WebSocket, SQLite FTS update trigger.
   result=engine, REST and MCP content update exist and directly use
   MemoryEngine.update_memory; CLI/WebSocket update surfaces do not exist.

6. Deterministic hash similarity probe
   exact base=1.0
   near candidate (base + " revised")=0.9460824235
   Expected create decisions: reject and review.

7. .\.venv\Scripts\python.exe -m py_compile tests\groundtruth\test_update_admission_invariant.py
   exit=0

8. .\.venv\Scripts\python.exe -m pytest --collect-only -q tests\groundtruth\test_update_admission_invariant.py
   4 tests collected in 8.91s
   exit=0

9. Initial characterization run
   .\.venv\Scripts\python.exe -m pytest -vv -s tests\groundtruth\test_update_admission_invariant.py
   direct engine=P0 behavior reproduced
   REST secret create/update/SQLite/FTS/recall/audit=P0 behavior reproduced
   REST exact create reject and near create review controls=passed
   MCP stdio=P0 behavior reproduced
   Harness stopped before two duplicate updates because a proposed safe-source
   fixture itself received admission 409. This was test-data similarity, not a
   product result.

10. Isolated REST rerun after replacing only the safe-source fixture with a
    deterministic low-similarity value.
    1 passed in 6.94s
    exact duplicate update=accepted
    near duplicate update=accepted

11. MCP persistence assertion strengthened to compare the stored embedding with
    hash_embed(raw synthetic secret).

12. Final evidence run
    .\.venv\Scripts\python.exe -m pytest -q tests\groundtruth\test_update_admission_invariant.py
    4 passed in 7.11s
    exit=0
    stdout copied to evidence/groundtruth/task-00A3/stdout/pytest.txt
    stderr copied to evidence/groundtruth/task-00A3/stderr/pytest.txt

13. Evidence result
    scenarios=25, pass=25, fail=0
    direct raw secret embedding/persistence/FTS/recall/audit=confirmed
    REST create redaction control=confirmed
    REST PUT raw secret embedding/persistence/FTS/recall/audit=confirmed
    MCP stdio raw secret persistence/embedding/FTS/recall/audit=confirmed
    exact create=reject; exact update=accepted
    near create=review; near update=accepted
    real_credentials=0
    product_code_changes=0

14. Ending evidence validation, git status and git diff --check are recorded
    after report generation.

No dependency install, product-code modification, remediation, commit, push,
PR, or merge was performed.

15. Evidence validation
    required_files=present
    JSON/JSONL parse=OK
    structured_text_trailing_whitespace=NONE
    live_like_credential_patterns=NONE

16. git status --short --untracked-files=all
    scope=accepted TASK-00A1, TASK-00A2 and TASK-00A3 outputs only
    product_code_changes=none

17. git diff --check
    exit=0
    output=(empty)
    Note: outputs are untracked, so structured-text whitespace was checked
    separately in step 15.
